# 规范 023: Bitcoin 生态适配器 (Bitcoin Ecosystem Adapter)

> 状态: 规划中 (V3 Target)
> 核心策略: **分层适配 (Layered Adaptation)**
> 目标: 覆盖 BTC L1 (Script)、BTC L2 (EVM)、Stacks (Clarity) 及 BitVM (未来)

## 0. 战略背景：为什么 Bitcoin 生态是"最后一块拼图"

### 0.1 市场价值

Bitcoin 是加密世界中**流动性最大**的资产，但开发者体验最为割裂：
- BTC L1: 原始 Script，几乎无人能写
- BTC L2: 各种 EVM 兼容链 (Merlin, B2, Rootstock)
- Stacks: 独特的 Clarity 语言
- BitVM: 前沿的链上计算方案

**Titan 的机会**: 用统一的 Zig 框架连接所有 BTC 生态层。

### 0.2 与 TON 的相似性

> **"是不是跟 TON 类似呢？"**
>
> **答案：一半是（BTC L1），一半不是（BTC L2）。**

| 生态层 | 架构 | Titan 策略 | 与 TON 相似度 |
| :--- | :--- | :--- | :---: |
| **BTC L2 (EVM)** | EVM 兼容 | 复用 Zig -> Yul | 0% (已有方案) |
| **BTC L1 (Script)** | 栈机 VM | Zig -> Miniscript | **90%** (几乎一样) |
| **Stacks (Clarity)** | LISP 解释器 | Zig -> Clarity | 50% (同步，简单) |
| **BitVM** | 逻辑门电路 | Zig comptime 展开 | 10% (全新领域) |

## 1. BTC Layer 2 (EVM 兼容链) - "白送的领地"

### 1.1 覆盖范围

目前主流的 BTC L2 都是 EVM 兼容链：

| 链 | 类型 | Titan 支持 |
| :--- | :--- | :---: |
| **Merlin Chain** | EVM L2 | ✅ (via Yul) |
| **B2 Network** | EVM L2 | ✅ (via Yul) |
| **Rootstock (RSK)** | EVM Sidechain | ✅ (via Yul) |
| **Bitlayer** | EVM L2 | ✅ (via Yul) |
| **BOB (Build on Bitcoin)** | EVM L2 | ✅ (via Yul) |

### 1.2 实现策略

**无需任何新工作！** 直接复用 `020_adapter_evm_native.md` 中的 Zig -> Yul 策略。

```bash
# 编译到任意 BTC EVM L2
titan-evm compile contract.zig -o contract.yul
solc --strict-assembly contract.yul -o contract.bin

# 部署到 Merlin/B2/RSK/Bitlayer
# 与部署到以太坊完全相同
```

**商业话术**: "用 Zig 编写合约，直接部署到 Merlin 和 Bitlayer，享受比特币的安全性。"

## 2. BTC Layer 1 (Native Script) - "跟 TON 极其相似"

### 2.1 架构对比

```
┌─────────────────────────────────────────────────────────────────┐
│                    TON vs Bitcoin L1 对比                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  TON (TVM):                                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  • 基于栈的 VM                                          │   │
│  │  • 汇编语言: Fift                                       │   │
│  │  • 复杂状态机 + 异步回调                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Bitcoin (Script VM):                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  • 基于栈的 VM                                          │   │
│  │  • 汇编语言: Bitcoin Script (OpCodes)                   │   │
│  │  • 简单逻辑: 转账、多签、时间锁                         │   │
│  │  • 非图灵完备 (更简单!)                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  关键差异: BTC L1 不需要处理异步回调，逻辑简单很多            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 中间语言选择：Miniscript

**Miniscript** 是 Bitcoin 世界的 "Fift" 或 "Yul"。它把晦涩的 OpCodes 封装成结构化表达。

```
原始 Bitcoin Script (痛苦):
OP_DUP OP_HASH160 <pubkey_hash> OP_EQUALVERIFY OP_CHECKSIG

Miniscript (清晰):
pk(owner_key)
```

### 2.3 映射关系

| Zig 代码 | 生成的 Miniscript | 说明 |
| :--- | :--- | :--- |
| `ctx.signer == owner` | `pk(owner_key)` | 签名校验 |
| `ctx.time > 2025` | `after(2025)` | 绝对时间锁 |
| `ctx.confirmations > 6` | `older(6)` | 相对时间锁 |
| `if (A && B) { ... }` | `and(pk(A), pk(B))` | 逻辑与 (多签) |
| `if (A \|\| B) { ... }` | `or(pk(A), pk(B))` | 逻辑或 |
| `threshold(2, [A,B,C])` | `thresh(2,pk(A),pk(B),pk(C))` | 门限签名 |

### 2.4 用户视角代码

```zig
const btc = @import("btc_sdk");

// 定义一个 2-of-3 多签 + 时间锁的 UTXO 条件
pub const VaultScript = struct {
    owners: [3]btc.PubKey,
    timeout: u32,

    pub fn spending_condition(self: *VaultScript) btc.Script {
        return btc.or_script(
            // 条件 1: 任意 2 个 owner 签名
            btc.threshold(2, self.owners),
            // 条件 2: 超时后单人可解锁
            btc.and_script(
                btc.after(self.timeout),
                btc.pk(self.owners[0]),
            ),
        );
    }
};
```

### 2.5 生成的 Miniscript

```
or(
    thresh(2, pk(owner1), pk(owner2), pk(owner3)),
    and(after(1735689600), pk(owner1))
)
```

### 2.6 编译流水线

```
Zig Source ──► Zig SDK ──► Miniscript ──► Policy Compiler ──► Bitcoin Script
                 │              │                │
            我们实现       人类可读          官方工具
```

**官方工具**: `rust-miniscript` 或 `bdk` (Bitcoin Dev Kit)

## 3. Stacks (Clarity) - "LISP 风格转译"

### 3.1 Clarity 特点

| 特性 | 说明 |
| :--- | :--- |
| **语法** | LISP 风格 (全是括号) |
| **执行** | 解释型，非编译 |
| **图灵完备** | 否 (故意限制，为了安全) |
| **异步** | 否 (同步执行) |

**好消息**: 比 TON 简单！没有异步回调地狱。

### 3.2 映射关系

| Zig 代码 | 生成的 Clarity | 说明 |
| :--- | :--- | :--- |
| `pub fn transfer(...)` | `(define-public (transfer ...))` | 公开函数 |
| `fn internal(...)` | `(define-private (internal ...))` | 私有函数 |
| `const x: u64 = 100` | `(define-constant x u100)` | 常量 |
| `var balance: u64` | `(define-data-var balance uint u0)` | 状态变量 |
| `if (a > b) { ... }` | `(if (> a b) ...)` | 条件 |
| `ctx.transfer_stx(amount)` | `(stx-transfer? amount tx-sender ...)` | STX 转账 |

### 3.3 用户视角代码

```zig
const stacks = @import("stacks_sdk");

pub const Token = struct {
    total_supply: u128,
    balances: stacks.Map(stacks.Principal, u128),

    pub fn transfer(ctx: *stacks.Context, to: stacks.Principal, amount: u128) !void {
        const sender = ctx.tx_sender;
        const sender_balance = self.balances.get(sender) orelse 0;

        if (sender_balance < amount) return error.InsufficientBalance;

        try self.balances.set(sender, sender_balance - amount);
        try self.balances.set(to, (self.balances.get(to) orelse 0) + amount);
    }
};
```

### 3.4 生成的 Clarity

```lisp
(define-map balances principal uint)

(define-public (transfer (to principal) (amount uint))
    (let (
        (sender tx-sender)
        (sender-balance (default-to u0 (map-get? balances sender)))
    )
        (asserts! (>= sender-balance amount) (err u1))
        (map-set balances sender (- sender-balance amount))
        (map-set balances to (+ (default-to u0 (map-get? balances to)) amount))
        (ok true)
    )
)
```

## 4. BitVM - "终极未来"

### 4.1 什么是 BitVM？

BitVM 试图在比特币上通过**逻辑门电路 (NAND Gates)** 来模拟任意计算：
- 将程序编译为巨大的 Merkle Tree
- 每个叶子节点是一个 NAND 门
- 通过欺诈证明实现链上验证

### 4.2 为什么 Zig 是完美选择？

| 需求 | Zig 能力 |
| :--- | :--- |
| **极致内存控制** | Zig 的显式内存管理 |
| **预计算大量电路** | `comptime` 编译时展开 |
| **生成 Merkle Tree** | 零运行时开销 |
| **位级操作** | 原生支持任意位宽整数 |

### 4.3 概念示例

```zig
// BitVM 电路生成器 (概念)
pub fn compile_to_bitvm(comptime logic: fn(u8, u8) u8) BitVMCircuit {
    comptime {
        var circuit = BitVMCircuit.init();

        // 在编译时将函数展开为 NAND 门电路
        // 每个算术操作 -> 多个 NAND 门
        // 生成 Merkle Tree 结构

        return circuit;
    }
}

// 使用
const add_circuit = compile_to_bitvm(fn(a: u8, b: u8) u8 {
    return a + b;
});
```

### 4.4 实现状态

| 阶段 | 状态 | 说明 |
| :--- | :---: | :--- |
| 研究 | 🔬 | BitVM 协议仍在演进 |
| 设计 | ⏳ | 等待协议稳定 |
| 实现 | ⏳ | 需要专门团队 |

**战略建议**: 关注但暂不投入，等 BitVM 协议成熟后再攻克。

## 5. 难度评估对比

| 目标 | 难度 | 原因 | 优先级 |
| :--- | :---: | :--- | :---: |
| **BTC L2 (EVM)** | ⭐ | 复用 Yul，零新工作 | P0 |
| **BTC L1 (Miniscript)** | ⭐⭐ | 逻辑简单，无异步 | P1 |
| **Stacks (Clarity)** | ⭐⭐⭐ | LISP 语法适配 | P2 |
| **TON (TVM)** | ⭐⭐⭐⭐⭐ | 异步 + Cell 树，最复杂 | P3 |
| **BitVM** | ⭐⭐⭐⭐⭐⭐ | 前沿研究，协议未稳定 | P4 |

**关键洞察**: 搞定 BTC 生态比搞定 TON **还要简单**！

## 6. 后端目标扩展

### 6.1 新增 TargetChain

```zig
pub const TargetChain = enum {
    // ... 现有目标 ...

    // ===== BTC 生态 =====
    /// BTC L2 (EVM 兼容) - 复用 evm_native
    btc_l2_evm,
    /// BTC L1 (Miniscript)
    btc_native,
    /// Stacks (Clarity)
    stacks,
    /// BitVM (未来)
    bitvm,
};
```

### 6.2 编译命令

```bash
# BTC L2 (EVM) - 复用现有 EVM 工具
titan-evm compile contract.zig -o contract.yul

# BTC L1 (Miniscript)
titan-btc compile contract.zig -o spending.miniscript

# Stacks (Clarity)
titan-stacks compile contract.zig -o contract.clar
```

## 7. 结论

### 7.1 BTC 生态纳入后的完整架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Titan "全链覆盖" 架构                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  路径 A: LLVM 原生编译                                          │
│  ├── Solana (SBF)                                               │
│  ├── Near/Cosmos/Polkadot (Wasm)                                │
│  ├── Arbitrum Stylus (Wasm)                                     │
│  └── CKB (RISC-V)                                               │
│                                                                 │
│  路径 B: 源对源转译                                              │
│  ├── TON ────────────► Fift ────────► TVM                       │
│  ├── EVM ────────────► Yul ─────────► EVM Bytecode              │
│  ├── BTC L2 (EVM) ───► Yul ─────────► EVM Bytecode  ← 复用!     │
│  ├── BTC L1 ─────────► Miniscript ──► Bitcoin Script  ← 新增    │
│  ├── Stacks ─────────► Clarity ─────► Clarity VM      ← 新增    │
│  └── BitVM ──────────► NAND Circuit ► Merkle Tree     ← 未来    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 战略总结

> **加上 BTC 之后，Titan 就是真正的 "All-Chain Framework"。**
>
> **没有任何一条有价值的链是覆盖不到的。**

| 生态 | 用户数/TVL | Titan 支持 |
| :--- | :--- | :---: |
| Ethereum + L2 | $100B+ TVL | ✅ (Yul) |
| Solana | 1000万+ 用户 | ✅ (SBF) |
| Bitcoin | $1T+ 市值 | ✅ (Miniscript/Yul) |
| TON/Telegram | 9亿用户 | ✅ (Fift) |
| Cosmos | 50+ 链 | ✅ (Wasm) |
| Polkadot | 100+ 平行链 | ✅ (Wasm) |
